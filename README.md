# Планировщик путешествий — REST API

RESTful API для управления путешествиями с полным набором CRUD-операций.  
Позволяет создавать, просматривать, обновлять и удалять путешествия, управлять компонентами (перелёты, отели и т.д.), отслеживать бюджет и маршруты.

---

## 🚀 Запуск проекта

1. Убедитесь, что установлен [Go](https://go.dev/dl/) (версия 1.22+).
2. Клонируйте проект и перейдите в папку:
   ```bash
   git clone <ваш-репозиторий>
   cd travel-api
3. Установите зависимости:
   ```bash
   curl -X POST http://localhost:3000/api/v1/trips \
     -H "Content-Type: application/json" \
     -d '{
       "id": "123",
       "destination": "Париж",
       "start_date": "2025-07-01T00:00:00Z",
       "end_date": "2025-07-10T00:00:00Z",
       "type": "отпуск",
       "budget": 100000,
       "components": []
     }'
4. Обновить путешествие
    ```bash
    curl -X PUT http://localhost:3000/api/v1/trips/123 \
     -H "Content-Type: application/json" \
     -d '{
       "destination": "Барселона",
       "budget": 120000
     }'
5. Удалить путешествие
    ```bash
    curl -X DELETE http://localhost:3000/api/v1/trips/123